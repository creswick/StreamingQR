package com.galois.qrstream.lib;

import android.content.Context;
import android.view.View;

public class TorrentBar extends View {
    public TorrentBar(Context context) {
        super(context);
    }
}